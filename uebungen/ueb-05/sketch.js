

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);


    // initialisiserung
  for (let i = 0; i < 10; i++) {
    // anweisungen
    ellipse(200 * i, height/2, 200, 200);
    

  }

}
