let posX = 0;
//let posY = 100

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);

  rect(posX, height/2, 50, 50)

  posX = posX + 1;

  if(posX < 350){
    
  }

  //if(posX == windowWidth-50){
  //  noLoop();
  //}

}
